# CMS 热门标签库（自动化打标签参考）

> 数据来源：CMS 后台文章列表（3 页截图，去重后 48 篇文章）统计的 105 个真实标签。
> 标签种数 **105**，总引用 **205** 次。

Agent 给文章生成标签时，**优先从本库挑选**（贴合站点现状），可再补充少量文章专属标签（合计 3-5 个）。

## 高频（≥5 次，11 个）

`办公软件`×17、`Excel`×9、`WPS Office`×8、`金山办公`×8、`WPS教程`×7、`Excel教程`×6、`办公技巧`×6、`WPS`×5、`LibreOffice`×5、`办公效率`×5、`免费办公软件`×5

## 中频（≥3 次，4 个）

`文档编辑`×4、`文档排版`×4、`wps表格`×4、`金山文档`×4

## 低频（≥1 次，90 个）

`空白页删除`×2、`在线办公`×2、`Office替代`×2、`免费软件`×2、`Microsoft 365`×2、`Word教程`×2、`excel技巧`×2、`云办公`×2、`在线编辑`×2、`Microsoft Office`×2、`办公软件对比`×2、`PPT`×2、`演示文稿`×2、`PPT教程`×2、`WPS AI`×2、`做表格`×2、`数据透视表`×2、`数据分析`×2、`WPS技巧`×1、`Word空白页`×1、`空白页`×1、`分节符`×1、`Word`×1、`分页符`×1、`Windows`×1、`文档处理`×1、`排版技巧`×1、`数据验证`×1、`下拉菜单`×1、`自动生成目录`×1、`Word目录`×1、`序号填充`×1、`效率工具`×1、`软件对比`×1、`WPS和Office哪个好用`×1、`办公软件推荐`×1、`Windows办公`×1、`Office`×1、`wps演示`×1、`视频插入`×1、`wps在线文档`×1、`在线协作`×1、`AI写作`×1、`文档管理`×1、`云端办公`×1、`PowerPoint`×1、`AI生成PPT`×1、`云表格`×1、`Gamma`×1、`AI PPT`×1、`博思AIPPT`×1、`AI做PPT软件`×1、`PPT工具推荐`×1、`简历`×1、`单元格`×1、`Office替代品`×1、`Office替代方案`×1、`微软365`×1、`Office激活码`×1、`正版软件`×1、`表格排版`×1、`共享文档`×1、`简历制作`×1、`删除重复项`×1、`数据清理`×1、`去重`×1、`办公文档`×1、`excel函数`×1、`表格技巧`×1、`数据处理`×1、`表格操作`×1、`电子表格`×1、`word文档`×1、`pdf转word`×1、`云端协作`×1、`PDF转换`×1、`WPS会员`×1、`WPS超级会员`×1、`WPS大会员`×1、`会员对比`×1、`WPS文字`×1、`PDF编辑软件`×1、`Adobe Acrobat`×1、`福昕PDF`×1、`PDF编辑器推荐`×1、`WPS PDF`×1、`ppt模板`×1、`Google Docs`×1、`Word替代品`×1、`办公技能`×1
